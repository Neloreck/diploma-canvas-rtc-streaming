import {createStyles} from "@material-ui/core/styles";

export const signInPageStyle = createStyles({
  content: {
    flexGrow: 24,
    width: "100%"
  },
  root: {
    height: "100%",
    width: "100%"
  }
});
