import {createStyles} from "@material-ui/core/styles";

export const headerBarStyle = createStyles({
  root: {
    backgroundColor: "red",
    flexBasis: "1rem",
    flexGrow: 1,
    height: "30px",
    width: "100%"
  }
});
