import {LazyLoadComponentFactory} from "@Main/view/utils/lazy_load/LazyLoadComponentFactory";

export const lazyLoadComponentFactory = new LazyLoadComponentFactory();

export {LazyLoadComponentFactory} from "@Main/view/utils/lazy_load/LazyLoadComponentFactory";
