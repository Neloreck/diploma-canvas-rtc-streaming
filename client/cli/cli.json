{
  "config": {
  },
  "scripts": {
    "clean": "npx rimraf ./target && echo 'Target cleaned up.'",
    "clean:build": "npx rimraf ./target/dist && echo 'Dist cleaned up.'",
    "purge": "npx rimraf ./target && rimraf ./node_modules && echo 'Project purged.'",
    "=============================================================================================================": "",
    "build-client:webpack": "npm run cli clean:build && ts-node ./cli/build/build.ts",
    "============================================================================================================": "=",
    "generate": "npx ts-node ./cli/generate/generate.ts",
    "===========================================================================================================": "==",
    "test": [
      "npm run cli lint",
      "npm run cli jest"
    ],
    "jest": "npx ts-node ./cli/test/test.ts",
    "lint": "npx tslint --project ./src/"
  }
}