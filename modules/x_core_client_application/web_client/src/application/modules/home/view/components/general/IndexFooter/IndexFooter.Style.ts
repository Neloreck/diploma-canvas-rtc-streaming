import { Theme } from "@material-ui/core";
import { createStyles } from "@material-ui/core/styles";

export const indexFooterStyle = (theme: Theme) => createStyles({
  root: {
    height: theme.spacing.unit * 10
  }
});
